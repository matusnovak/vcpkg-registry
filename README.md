# Matus Novak's custom vcpkg registry

Contents for the `vcpkg-configuration.json`:

```
{
  "registries": [
    {
      "kind": "git",
      "repository": "https://github.com/matusnovak/vcpkg-registry",
      "packages": [ "msgnet" ]
    }
  ]
}
```
